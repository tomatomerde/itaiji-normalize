# itaiji-normalize について（技術メモ）

このファイルはプロジェクトの技術的事実（目的・壊してはいけない価値・データ出典・非対応範囲）を
まとめたもの。人間の貢献者向けの不変条件は `CONTRIBUTING.md` にもある。

## 目的

MJ縮退マップを用いた異体字の縮退変換・等価クラス判定・名寄せキー生成・異体字展開（候補列挙）を
提供する TypeScript/npm ライブラリ。依存ゼロ、Node 18+/ブラウザ/Cloudflare Workers で動作し、
全データ同梱のオフライン構成とする。

## 差別化点（＝壊してはいけない価値）

- 公的データ（MJ縮退マップ）由来の**根拠付き**対応（既存 OSS の itaiji はハードコード辞書・根拠なし）
- IVS（異体字セレクタ）を入力として受理し、正しく解釈する
- 等価クラス判定（2つの文字/文字列が異体字関係にあるかの判定）
- 曖昧さを隠さない API: 縮退候補が複数/ゼロのケースを明示的に返し、勝手に一意化しない。
  一意選択は `unique` フィールドとして別途提供し、アルゴリズムと限界をドキュメント化する

## データ出典

- 文字情報技術促進協議会/IPA「MJ縮退マップ」（CC BY-SA 2.1 日本）。IPA の著作物であることを明記する
- 原本ファイルはリポジトリ内にスナップショットとして保持（取得日・バージョン・URL・ハッシュを記録）。
  ビルド時・実行時に外部から取得しない
- コードは MIT、同梱データは CC BY-SA 2.1 JP の二重ライセンス構成。SA 継承がデータ派生物に及ぶ旨を README に明記

## 非対応範囲

- ホスト型 API・Web サービス化
- 一意な逆変換 API（新字→特定の旧字）。逆方向の一意選択には根拠が存在しないため提供しない（README に FAQ として理由を明記）
- 法務省誤字俗字・正字一覧の統合（v1 では調査報告のみ）
- Python 版（TypeScript 版の反応を見てから）
- 戸籍・法務・金融用途での同一性判断は保証しない（免責）
