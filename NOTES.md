# NOTES

作業再開・引き継ぎ用のメモ。調査の詳細は `docs/phase0-report.md`(冒頭に
フェーズ1での訂正表あり)、変更の経緯は各コミットメッセージを参照。

## 引き継ぎ

**このリポジトリは 2026-08-07 に作り直されている。**
公開前の履歴整理(コミット author の個人メール除去)は force push では消せないため、
旧リポジトリを退避して空のリポジトリを作り直し、書き換え済みの `main` だけを push した。
コードは1バイトも変わっていない(tree は `da6a082` のまま)。

その副作用として、**この文書と履歴に出てくる `#N` は旧リポジトリの PR 番号で、
現在のリポジトリの PR 番号とは対応しない**。現在のリポジトリの PR は0件から始まる。
GitHub はコミットメッセージ中の `#N` を自動リンクするので、いま押しても 404 になり、
**今後このリポジトリで PR を開くと、過去の `#1` が無関係な PR を指すようになる**。
経緯を追うときは番号ではなく `git log` の件名で辿ること。
同じ理由で、旧リポジトリ側の SHA を引いている記述も解決しない
(`48ec1b0` = 旧 PR #1 の squash マージ。現在の履歴では `b937df3` に当たる)。

**現在地**: `main` に v0.1.0 相当(旧 PR #1)、旧 PR #2、そして**ラウンド6**
(旧 PR #3 のレビュー + `pickUnique` 検証 + その対応)がマージ済み。
**npm 公開済み**: `itaiji-normalize@0.1.2`（最新。0.1.0 は 2026-08-10、0.1.1 は
2026-08-12、0.1.2 はラウンド9の修正で 2026-08-13、いずれも provenance
attestation 付き）。詳細は下の「ラウンド9」参照。

**先に読むべき事故**: PR #2 と PR #3 は**互いを含まない並行ブランチ**で、
両方が NOTES 上で「ラウンド5」を名乗り、**同じ指摘に別々の解**を出していた
(推論エッジの扱い)。ラウンド6でこれを統合した。経緯と統合方針は
「ラウンド6」節。**同じ事故を繰り返さないために、着手前に必ず
`git log --oneline --all --graph` で並行ブランチの有無を見ること。**

**次にやると価値が高いこと**: 「まだ検証していない領域」節の消し込み
(推論エッジの個別妥当性、常用漢字タイブレーク調査など)。これまで
**10ラウンドすべてで実の指摘が出ている**(下記の履歴参照)。特に「ある修正が
別のバグを露出させる」パターンが3回起きているため、`src/toMatchingKey.ts` /
`src/reduce.ts` / `src/ivs.ts` / `scripts/build-tables.ts` を触ったら必ず
独立検証を回すこと。**文書の統計値はラウンド9で
`test/data-invariants.test.ts`(「文書が主張する統計値」)に固定した——データを
更新したらこのテストが落ちるので、README(英日)と JSDoc の数値を実測し直して
両方を同じ PR で更新すること。**

**動かし方**:

```sh
npm ci
npm run build:tables   # data/snapshot/ から src/generated/tables.ts を再生成
npm run build          # tsup で ESM + CJS + .d.ts
npm test               # vitest(111件)
npm run typecheck
./scripts/verify-snapshots.sh   # 同梱スナップショットを PROVENANCE.md のハッシュと照合
npm run test:browser   # headless Chromium で dist を実行(要 npm run build)
npm run test:workers   # workerd(Cloudflare Workers)で dist を実行(同上)
npm run test:dist      # 既存111件のテストスイートを dist/index.js (ESM) に対して実行(要 npm run build)
npm run test:dist:cjs  # 同上、dist/index.cjs (CJS) に対して実行
```

ブラウザ検証で使う Chromium は `scripts/verify-browser.mjs` が解決する。環境変数
`CHROMIUM_PATH` があればそれを、無ければ既定のプリインストール先を見て、どちらも
無ければ Playwright 自身の解決に任せる(CI は `playwright install` 後のこの経路)。
Playwright が解決する版とリビジョンが合わない実行環境があるための順序で、
詳細は同ファイルの `resolveExecutablePath` のコメントにある。

**このプロジェクトの作法**(CLAUDE.md の要求。守らないと意味がない):
修正したら**修正前に再現し、修正後に消えること**を両方確認する。テストを足したら
**対象コードをわざと壊してそのテストが落ちるか**を確認する(落ちないテストは
何も守っていない)。確かめていないことは「確かめていない」と明示する。

## 現在の状態

PR #1(v0.1.0 相当)と PR #2(ブラウザ/Workers の実行検証 + ラウンド5修正)は
`main` にマージ済み。PR #3 はラウンド6が内容を取り込んだためクローズ済み。
ラウンド6(PR #5)もラウンド7のレビューを経て `main` にマージ済み。
`itaiji-normalize@0.1.0` は 2026-08-10 に npm へ公開済み。その後ワークフローを
trusted publishing（トークンレス OIDC）に切り替えた。

レビューは7ラウンド行った(実装直後の2周 + リリース想定の監査1周 + その修正の
検証1周 + 公開前の総ざらい1周 + PR #3 のレビューと統合1周 + PR #5 のレビュー1周)。
以下は各ラウンドで見つかり、修正した内容。いずれも**修正前に再現し、
修正後に消えることを確認**し、追加したテストは対象コードをわざと壊して落ちること
まで確認している。

**繰り返し起きたパターン**: ある修正が、それまで別のバグに隠されていた問題を
露出させることが2回あった(IVSキーの大文字小文字を直したら互換漢字の非収束が
現れ、SVS範囲を直したら単一文字APIの挙動が変わった)。この領域を触ったら
必ず独立検証を回すこと。

### ラウンド1・2(実装直後のレビュー)

1. **CRITICAL 非収束**: 縮退テーブルのキーに `対応するUCS`(実装を持たない
   場合もある緩い対応)を使っていたため、無関係なエントリの候補が混入し、
   歯↔齒・炉↔爐 が互いに循環していた。`実装したUCS` に変更して解消
2. **CRITICAL 多段階非収束**: 30,395件中21,116件が `reduce()` 1回では安定
   しないと判明。`toMatchingKey` に不動点反復(サイクル検出付き)を実装
3. **CRITICAL IVSテーブルが死んでいた**: TSV の大文字16進をそのまま保存する
   一方 `ivsKey()` は小文字を生成しており、全キーが不一致で常に基底へ
   フォールバックしていた。小文字化で解消
4. **CRITICAL 出力側の非冪等**: 縮退先が CJK 互換漢字(reduce 的には不動点
   だが NFKC で分解される)になる場合、キーが正規化されず 㙇 と 琢 が別キーに
   なっていた。各ホップの結果を都度正規化して解消。全40,366キーの掃引テストを追加
5. HIGH `splitUnits` が O(n²)(4万文字で11秒)→ 単一走査に書き換え(5ms)
6. MEDIUM SVS 列を読んでいなかった / `resolvedVia` 未実装 / `"cycle"` と
   `"ambiguous"` の混同 / 未解決時に変異セレクタを落とす / テストの空配列に
   よる無条件通過 / CI がテーブルのドリフトを検知しない

### ラウンド3(リリース想定の監査)

7. **CI が2ジョブとも赤**だった。`test` は Node 18 で vitest 4 が起動不能
   (rolldown が Node 20.12+ の `styleText` を要求)。`drift` は
   `DEV_STANDARDS_TOKEN` 未設定で `actions/checkout` が hard fail。
   → テストは Node 20/22 マトリクスに変更し、**`smoke-node18` ジョブを新設**
   して公開 tarball を Node 18 で `require()`/`import()` する実証に置き換えた。
   drift は secret の有無を判定してスキップするようにした
8. **CRITICAL U+FE0F を無言削除**: SVS 範囲を U+FE00–FE0F としていたため
   絵文字表示セレクタを食べて捨てていた(BMP掃引で20,335文字が脱落)。
   MJ が実際に使う SVS は U+FE00/U+FE01 だけと確認した上で U+FE00–FE0D に限定
9. **`unresolved` が機能していなかった**: MJ 外の文字をすべて報告するため
   住所1行で34文字中33文字が載り、`if (unresolved.length)` が常に発火していた。
   漢字だけを報告するよう変更(範囲の妥当性はテーブル全キーとの照合テストで固定)
10. **非文字列入力の誤答**: `reduce(["崎"])` が例外にならず、配列を `input` に
    echo した上でもっともらしい候補を返していた。4関数すべてに型ガードを追加
11. **変異セレクタ複数連結で入力に無い IVS を捏造**していた。単位としてまとめて
    消費し、2つ以上なら縮退せず素通し + `"unsupported-sequence"` 報告に変更
12. **tree-shaking が全く効かず**全利用者が 550KB(gzip)を負担していた。
    生成テーブルに `/* @__PURE__ */` を付与し、部分 import で 270–281KB に半減
    (API 全体を使う場合は 552KB のままで、README に正直に明記)
13. `toMatchingKey` が捨てる `candidates` を毎ホップ生成していた →
    内部パスを分離し 224ms → 149ms(10万件)
14. **統計値がパイプライン修正前のまま**だった(直接カバー率 74.5% → 実測
    71.4% など)。README(英日)と phase0 報告の訂正表を実測値に更新
15. **LICENSE-DATA が公開物に存在しないファイルを列挙**していた。実際に
    CC BY-SA データを含むのは `dist/index.js` / `dist/index.cjs` なので
    そちらを明記し、`PROVENANCE.md` を同梱ファイルに追加

### ラウンド4(ラウンド3の修正の検証)

16. **HIGH `toMatchingKey` の options が未検証だった**: `text` はガードしていたが
    `options` は素通しで、`"NFD"`/`"NFKD"` が `normalize()` にそのまま渡っていた。
    分解形で作ったキーは既定(NFKC)のキーと絶対に一致しないので、例外にならない
    まま永久にマッチしない = ガードを入れた目的そのものの失敗だった。
    正当な3値のホワイトリストに変更。`toMatchingKey("崎", "NFC")` のように
    options に文字列を渡すケースも黙って NFKC にフォールバックしていたので拒否する
17. **MEDIUM 部首ブロックが `unresolved` の対象外だった**: `src/cjk.ts` の範囲に
    CJK部首補助(U+2E80–2EFF)と康熙部首(U+2F00–2FDF)が無く、⺅(U+2E85)は
    NFKC マッピングも持たないため**どの正規化モードでも黙って通り抜けて**いた。
    両ブロックと拡張J(U+323B0–3347F)を追加。IDC(U+2FF0–2FFF)と
    CJK Strokes(U+31C0–31EF)は構造記法・部品であり漢字に畳まれることを
    期待する対象ではないので、理由をコードに書いた上で意図的に除外
18. **MEDIUM 抽出器が実在するシーケンス4件を落としていた**(ラウンド3以前からの
    既存バグ): 一覧表の2行が1セルに複数シーケンスを `;` 区切りで持っており
    (MJ059399 の `2B9E4_E0100;535A_E010A`)、セル全体を1キーにしていたため
    𫧤・博 の該当シーケンスがどの入力からも引けなかった。分割して 9,971 → 9,973 キーに。
    生成器に「整形式でないキーは emit を拒否する」ガードを追加し、
    全キーを公開API経由で引き直す到達性テストも追加
19. ドキュメントの精度: バンドルサイズ表は丸め境界にあるため概算であることと
    計測コマンドを明記。スループットは1台の楽観値ではなく複数マシンでの
    0.5〜0.7M/s のレンジに修正

### ラウンド5(公開前・未検証領域の総ざらい)

20. **HIGH 渡邉 が 渡辺 と一致しなかった**: 邉 の候補 辺/邊 が同点で
    `pickBest` が選択を拒否 → `"ambiguous"` として素通ししていた。だが 邊 自身も
    辺 に縮退するため**どちらの枝も 辺 に至り、選択は結果を変えない**。
    渡邊 は一致するのに 渡邉 は一致しないという、どちらか一方に倒すより悪い状態だった。
    全ての拮抗枝をたどり一致した場合のみ採用するよう変更(当て推量ではなく証明)。
    拮抗898字中345字が解決、残り553字は本当に別の不動点に至るので `"ambiguous"` のまま。
    `reduce()` は1段階APIのままで `unique: null` を返す(役割分担は不変)
21. **MEDIUM `getVariants` が根拠を借用していた**: 同一 MJ 字形の候補同士を結ぶ
    エッジに両候補のビットマスクの OR を載せていたため、`getVariants("斉")` が
    資 を `basis: moj-notice-582-appendix-4` で返していた。告示が言っているのは
    「齍 は 斉 または 資 と書ける」であって、斉 と 資 が交換可能とは言っていない。
    30,766エッジ中3,000件(9.8%)がこの種の推論。新しい `Variant` 型の
    `inferred` フラグで区別するようにした。**根拠付きであることが本パッケージの
    差別化点そのものなので、件数以上に重い**
22. **LOW(潜在) `extract-mji.py` が `<rPh>`(ふりがな)を除外していなかった**:
    `<si>` 配下の全 `<t>` を連結していたため、ルビを持つ共有文字列では
    値にルビが混入する(素朴なパースだと `実装なし` が `実装なしジッソウ` になる)。
    現スナップショットでは該当2件がどちらも抽出対象列に載らないため TSV は
    バイト一致のままだが、データ更新時に黙って値が壊れる経路だった

このラウンドで**確認できた(問題なし)**もの: `extract-mji.py` の全58,862行が
独立実装とバイト一致 / 主要な異体字姓10グループ中9グループが期待通りの挙動
(齋 と 斉 を別扱いにしているのも正しい) / ~~`pickUnique` の順位・ホップ数の
優先順は MJ データの意図と整合~~(**ラウンド6で撤回**: IPA のリファレンス実装は
JIS包摂規準を最優先・告示582の順位を最後に置いており、本ライブラリとは
両端が逆。「整合している」という当時の判断は実測に基づいていなかった)/
README 日英15セクションが1対1で一致 / 新しいブラウザ・Workers 検証スクリプトの
assert が意味を持つこと(Node組み込みAPIの注入・既定正規化の変更でいずれも落ちる)

### ラウンド6(PR #3 のレビュー、`pickUnique` の検証、そして PR #2/#3 の統合)

実装したのとは別セッションによるレビュー。数値はすべて実際に動かした実測。

#### 事故: PR #2 と PR #3 が分岐していた

両方が main から分かれた独立ブランチで、互いを含まず、両方が「ラウンド5」を
名乗っていた。しかも**同じ指摘(共候補エッジが根拠を偽っている)に別々の解**を
出していた:

| | PR #2(= 現 main) | PR #3 |
|---|---|---|
| 推論エッジ | 全部返し `inferred: true` で区別 | 既定で除外し `includeCoShrinkTargets` で opt-in |
| `basis` | 両候補の OR のまま(`inferred` で警告) | `co-shrink-target` 単独に差し替え |
| `isVariant("井","牛")` | true | false |
| `isVariant("猫","貓")` | true | **false** |

PR #2 だけが持っていたもの: **渡邉→渡辺 のマッチング修正**、browser/workerd の
**CI 検証**、`toMatchingKey` の tie 収束。PR #3 だけが持っていたもの:
`normalized`、セレクタ2個ガード、options 検証、`verify-snapshots.sh`、
CI の TSV 再現性検査。8ファイルで衝突した。

**統合方針**: PR #2 を先に main へマージし(変更が深い方を土台にする)、
その上に PR #3 系を載せた。**推論エッジは main の `inferred` を採用し、
PR #3 の「既定で除外」は捨てた** —— レビューで実測した偽陰性(下記)が理由。
代わりに `isVariant` に `{ includeInferred?: boolean }`(既定 `true`)を足し、
PR #3 が塞ごうとした穴(井-牛 を外す手段が無い)も塞いだ。

#### レビューの指摘(PR #3 に対して)

1. **HIGH 共候補を既定で除外するのは過剰**。除外される 3,000 対のうち、
   PR #3 が根拠に挙げた「告示582の順位つき代替」由来は **1,427 対 (47.6%)** で、
   残る 1,573 対は**異なる根拠カテゴリの間**で生じており順位が存在しない。
   さらに MJ は縮退が必要な字にしか縮退関係を記録しないため、両方が既に
   JIS X 0213 にある新旧字体は推論エッジでしか繋がらない。結果
   `isVariant` が false になる妥当な対: 猫/貓・摂/攝・併/倂・靱/靭・桝/枡・
   帚/菷・籐/籘・嘔/呕・答/荅(手元の34対サンプル中9対)。31文字は隣接字が
   全部推論エッジ。3,000対中 2,947 は本物のエッジ2ホップで到達できるが
   **53対は到達できない**。→ 統合では既定除外を採らず、オプション化した
2. **LOW 根拠ビットゼロのエッジを黙って許していた** → `addEdge` が投げるように
3. **LOW `verify-snapshots.sh` の awk が `## ` 見出しを無条件にファイル名として
   拾う** → `## <一語>` の見出しだけを見るように

#### `pickUnique` の検証(「まだ検証していない領域」から一部消し込み)

**当初は「外部資料に到達できず未完」としたが、後から別ルートで完了した。**
調査に使った作業環境から moji.or.jp / moj.go.jp へ直接接続できず、WG資料3 の
PDF 自体は取得できていない。しかし **IPA 自身のリファレンス
実装がそのアルゴリズムの実体**であり、GitHub 経由で読めた:
`mandel59/mj2jisx0213`(MIT、Copyright (c) 2015 IPA。`MJ2JISX0213.es` が
IPA 版、`unique-map.sql` が同等の SQL 移植)。PDF は「このプログラムの
アルゴリズムの説明」なので、プログラムそのものの方が一次資料に近い。

**これにより未検出のバグが1件出た(下記「別字」)。** 到達できた文書からは:

- MJ 公式の説明は「縮退先の候補が複数示されている文字については、**その文字が
  使われている状況を勘案し、実際の縮退先を判断する**といった対応が必要」で、
  一意選択を**データではなく利用者の文脈判断**と位置づけている。選び方の例は
  「常用漢字に属するもの」「画数の小さいもの」「JISコードの最も若いもの」であり、
  **順位やホップ数を使えという指示はどこにも無い**。README の「独自ヒューリス
  ティック」という位置づけは正しい。裏を返すと、順位/ホップ数を使う設計に
  外部の裏付けは無い
- mandel59/mj2jisx0213 の README はアルゴリズムを「平成27年度第一回文字情報基盤
  WG資料3『縮退マップについて』p.7〜8」に委ねている。この PDF は取得できていない
  (`http://mojikiban.ipa.go.jp/contents/2015/09/20150925_5.pdf`。
  ホスト自体が廃止され DNS も引けない。moji.or.jp 側の現行 URL も 403)

#### リファレンス実装との突き合わせ(実施済み)

**優先順位が本ライブラリとほぼ逆だった。** IPA 版 `MJ2JISX0213.es` の順序:

1. **JIS包摂規準・UCS統合規則** —— あればこれで確定(最優先)
2. 法務省戸籍法関連通達・通知 —— まず**別字の除外**、次に 民二5202号 正俗表、
   次に 戸籍統一文字情報 親字・正字 を最小ホップ数で
3. **法務省告示582号別表第四** —— 第1順位を優先(最後の手段)
4. 残った同点は 常用漢字 → 人名用漢字(JIS水準の小さい方)で解く

本ライブラリの `pickUnique` は tier 0 = 告示582の順位、tier 1 = 戸籍のホップ数、
tier 2 = その他(JIS包摂を含む)なので、**両端が入れ替わっている**。
これは目的の違いから説明できる: リファレンスは JIS X 0213 への変換表を作るので
「既に表現可能」で話が終わるが、本ライブラリは名寄せキーを作るので 㐂→喜 と
畳むことこそが目的になる。**意図的な差として README(英日)に明記した。**
ただし PR #2 の説明にあった「`pickUnique` の rank-then-hop はデータの意図に
一致している」という記述は**実測に照らして誤り**であり、撤回する。

なお 常用漢字/人名用漢字によるタイブレークは本ライブラリには無い。
`mji.00602.xlsx` の「漢字施策」列を読めば実装できる(未着手)。

#### 発見したバグ: 別字を除外していなかった(HIGH、修正済み)

民一2842号通達別表 誤字俗字・正字一覧表 の `付記=別字` は、通達が
**「これは別の文字だ」と明言している印**。IPA 版は該当 UCS をその MJ 字形の
**全カテゴリ**から除外する(`MJ2JISX0213.es` の 2.1)。本ライブラリは `付記` を
一切見ていなかったため、**別字を持ち素の文字として引ける 113 字のうち 96 字が、
まさに「別字」とされた相手に畳まれていた**(㐲→伏、㕍→雁、㬌→景、䇦→英、
䝷→智、䢖→建)。「推測でデータを捏造しない」という本プロジェクトの原則に
真正面から反する挙動だった。

修正: `build-tables.ts` が `付記=別字` の UCS を全カテゴリから除外する。
候補 314 件が落ち、テーブルは 30,395 → 30,344 キー、変異シーケンス
9,973 → 9,946 キー、エッジ 30,766 → 30,650(推論 3,000 → 2,999)、
推論エッジのみの文字 31 → 34 に変わった。96 → **1**。
残る1件は 腈→暒→晴 という2ホップの連鎖(腈 の entry では 晴 が別字だが、
暒 の entry は 晴 への縮退を持つ)。IPA 版は単段変換なのでこの状況が起きない。
修正前に再現・修正後に消えることを確認し、除外を外す変異でテストが3件落ちる
ことも確認済み。

データ内部からは実測できた:

- **告示582の順位はタイを作らない**: 全40,368キーで tier 0 が同点になるのは
  **0件**。順位が全順序として設計されている裏付け。同点は tier 1(ホップ)248件、
  tier 2 581件のみ
- **順位とホップが別の勝者を出すのは 251 MJ字**。うち **154 で順位側がより
  一般的な JIS 水準の字**を選び(弐・却・厨・双・台・穏・寝・尽・斉・国・蚕・写)、
  48 が同水準、**49 で順位側がより稀な字**を選ぶ(㓮: 順位→雕 / ホップ→彫、
  㔫: 順位→尢 / ホップ→左)。tier の順序は 3:1 で妥当だが一律ではない
- **キーごとの候補は必ず単一のMJ字由来**: 実装したUCS が2つ以上のMJ字で重複する
  キーは 0/30,395 件。「別のMJ字の順位表どうしを比較してしまう」懸念は
  **存在しない**(疑って実測し、白)
- **JIS包摂規準の根拠は一意選択に一切効かない**。JIS包摂候補 10,273 件のうち
  10,054 件は「自分自身」(＝縮退不要の宣言)で順位もホップも持たないため
  最下位ティアに落ちる。結果 **1,277 キーで自分自身を捨てて別字を選び**
  (㐂→喜、㠀→島。名寄せとしては妥当)、**54 キーで自己候補と拮抗して null**
- **`reduce().unique` は CJK互換漢字になりうる(165キー)**。`reduce("楳").unique`
  は U+FA44 で NFKC では分解される。`toMatchingKey` は無害だが、`unique` を
  直接キーに使うと 楳 と 梅 が別キーになる

#### 対応(このラウンドで実装した分)

- `isVariant` に `{ includeInferred?: boolean }`(既定 `true`)を追加。
  `getVariants` はフラグを返せるのでオプションを持たない
- README(英日)に `includeInferred` の節を追加し、**厳しい側にすると何を失うか**
  (猫/貓 等5対の実例、31文字)を明記。`unique` の節に、MJ が一意選択の手順を
  定めていないこと・互換漢字・自己候補を追記。`src/reduce.ts` の JSDoc にも同内容
- `toMatchingKey` が `normalized` を返す(PR #3 由来)。`unresolved[].index` が
  入力長を超えうることを型定義と README に実例つきで明記
- `isVariant`/`getVariants` がセレクタ2個以上を投げる(PR #3 由来、`src/unit.ts`)
- options の実行時検証(PR #3 由来、`src/options.ts`)
- `addEdge` が根拠ビットゼロのエッジを拒否
- `scripts/verify-snapshots.sh` と CI の TSV 再現性検査(PR #3 由来)

追加・改修したテストは**すべて対象を壊して落ちることを確認済み**(詳細は
コミットメッセージ)。生成テーブルは再生成して main とバイト一致を確認。

## 決定済みの設計判断

- **`"cycle"` の23字は未解決のまま報告する**(2026-08-05、所有者判断)。
  址/阯、雕/鵰、羐/羑、輀/轜、㿉/㿗 などは互いに縮退し合って安定しない。
  うち14字は「自分自身」も候補に持つ(JIS包摂規準)ため循環内に安定形は存在するが、
  **両方の字が自己候補を持つ**ため、どちらを代表にするかの根拠が無い。
  名寄せキーとしては循環内の最小コードポイントを正規形にすれば 址 と 阯 を
  マッチさせられるが、それは根拠のない代表字を名乗ることになるので採らない。
  `unresolved` に `reason: "cycle"` として報告されるため、必要な利用者は
  呼び出し側で対処できる。「曖昧さを隠さない」という本プロジェクトの差別化点に一致する
- **テーブルのコンパクト再符号化は v1 では見送り**。現状 gzip 約562KB で
  目標(1MB未満)は満たしており、README の Roadmap に未実装として明記済み

## 人間の操作待ちの項目

### 完了済み(2026-08-05)

- ~~**スナップショット2点の SHA-256 照合**~~ **完了**。「配布元からの
  ダウンロード原本 / `PROVENANCE.md` の記録 / リポジトリ内の実ファイル」の
  **3ソースが完全一致**することを確認済み
- ~~**旧 PR #1 のレビュー・マージ**~~ **完了**(squash merge。再作成後は `b937df3`)
- ~~**ドリフト検査用シークレットの設定**~~ / ~~**ドリフト検査ワークフローの hard fail**~~
  — **どちらも仕組みごと廃止された(2026-08-09)。** 当時は案件ごとに PAT
  (`DEV_STANDARDS_TOKEN`)を持たせて原本を読みに行っていたが、PAT は失効し、失効した1件が
  実際に別案件の CI を落とした。その後ハッシュ照合による配布方式に置き換わり、さらに
  **2026-08-12 に配布そのものが廃止された**——CLAUDE.md はプロジェクト技術情報だけになり、
  照合対象が消えたため `check-common-integrity.yml` ごと撤去。`DEV_STANDARDS_TOKEN` が
  まだ登録されていれば削除してよい

### 残っているもの

- **`v0.1.2` の git タグと GitHub Release が未作成（npm への公開自体は完了）。**
  0.1.2 は release ワークフローの workflow_dispatch（dry_run=false、run
  `31654224256`）で 2026-08-13 に publish された。レビューを行った作業環境の
  push 権限が作業ブランチに限定されており、タグを push できなかったため
  （HTTP 403、組織ポリシー）。次の3行を人間が実行すればタグ push の run が
  「npm に既にある」ことを検知して publish をスキップし、GitHub Release だけを
  作る（ワークフローが文書化している再実行経路）:
  ```sh
  git fetch origin main
  git tag v0.1.2 b685c687d572515c23333a06f33d5dce824d29a8
  git push origin v0.1.2
  ```
- **`NPM_TOKEN` は削除済み（2026-08-12）。** trusted publishing 経由のリリースが
  `v0.1.1`（run `31558135329`）で実際に通り——
  `Signed provenance statement with source and build information from GitHub Actions`
  がログに出ている——戻り先として残す理由が消えたため。**人間が3案件の Actions secret を
  消し、npm 側のトークンも revoke したとの報告。** セッションからはシークレット一覧を
  読めないので、こちらで実物を確認したわけではない（`DEV_STANDARDS_TOKEN` の削除と同じ扱い）。
  `release.yml` は3案件とも `NPM_TOKEN` を参照していないので、リリースは壊れない。
  **代わりに、publish は npmjs.com 側の trusted publisher 登録だけに依存する状態になった**——
  登録を消したり `release.yml` をリネームしたりすると、戻り先が無いのでリリースが止まる
  - 2026-08-11 に足した `scripts/assert-npm-version.sh`（publish 直前の npm 版の再確認、
    3案件でバイト一致）は、`v0.1.1` の実行では **12.0.2 のまま**通った。最後の
    `setup-node` が tool cache の同じ Node 22.23.1 を選び直したためで、予想どおりの挙動。
    **つまり「同梱 npm (10.9.x) に戻る」現象自体は、実機ではまだ一度も観測されていない。**
    ガードが仕事をしたのではなく、ガードが要る状況がまだ起きていない、というのが正確
- ~~**マージ済みブランチが6本残っている**~~ **完了**（2026-08-12 の実確認で、
  リモートブランチは `main` のみ）
ブランチ保護と GitHub の description / topics は 2026-08-10 に設定済み。保護の内容は
`main` への直 push 禁止、required checks は `test (20)` / `test (22)` / `browser` /
`workers` / `smoke-node18`、`strict: false`、`enforce_admins: true`、承認0件。
`integrity`（2026-08-12 に撤去済みの `check-common-integrity.yml`）は required に
入れていなかった——`paths:` フィルタ付きのチェックを required にすると、そのパスを
触らない PR が「Expected — waiting for status」で永久にマージできなくなるため。
教訓として残す: 保護を張り直すときも、パスフィルタ付きワークフローを required にしないこと。

## ラウンド7(PR #5 のレビュー)で残った判断

レビュー自体は完了し、PR #5 はこの3点を**残したまま**マージした。
1と2は**ラウンド10で再現を試みたが再現しなかった**ので、判断材料として使えない:

1. ~~**R2 による48字の自己停止を許容するか。**~~ **ラウンド10で再現できず。**
   「48」に一致する定義を約15通り試したが、どれも48にならない(IPA 移植で
   規則2により自己変換される UCS キーは0件、別字修正で自己停止するように
   なったのは38キー/39字 など)。しかも当時の記述「現在の実装は止めずに縮退を
   続ける」は、最も近い38件については**逆**(現在は止まっている)。
   **旧リポジトリのレビュー原文が回収できない限り、この項目は判断できない。**
   着手するなら、まず再現可能な定義で書き直すこと
2. ~~**𠀍 の回帰を許容するか。**~~ **ラウンド10で再現できず。** 𠀍(U+2000D)の
   エントリと挙動(→世、解決)は v0.1.0 から現在まで不変で、別字修正の影響も
   受けていない。ただし**恒常的な不整合**は実在する: UCS統合規則上は同一字の
   はずの 𠀍 と 丗 でキーが揃わない(𠀍→世 / 丗 は拮抗)。これは下の
   「非自己の JIS包摂候補」の論点と同じ根で、そちらに集約した
3. ~~**`src/reduce.ts` の 251/154/49 という主張は未検証。**~~ **ラウンド9で決着**:
   251 は別字修正前の旧テーブルの実測値で、現テーブルでは 248(再計算して
   テストに固定)。154/49 の「JIS 水準の一般性」は判定基準がリポジトリ内から
   再現できないため、数値ごと文書から撤去した(検証済みの 㓮 の実例だけ残した)

## ラウンド8(2026-08-11、公開後レビュー)

3案件横断のレビュー。このリポジトリで動かして確認したこと:

- `npm ci` → `build:tables` 再生成で **`src/generated/tables.ts` に差分ゼロ** →
  build → テスト 91/91 → typecheck → `verify-snapshots.sh` が
  `PROVENANCE.md` のハッシュと一致、まで通した
- README のコード例(`reduce("﨑")` / `isVariant` / `getVariants("崎")` の5件 /
  `toMatchingKey("田中﨑")`)を**ビルド成果物に対して実行**し、記載どおりの出力を確認
- 「既知の制約」のバンドルサイズを実測し直した(`esbuild --bundle --minify --format=esm`
  + gzip): isVariant 286KB / reduce 272KB / toMatchingKey 273KB / 全体 559KB。
  表の「約290/270/271/562KB」と数KB以内で一致しており、表の但し書きの範囲内

構成の指摘に対応した内容は `0.1.1`（2026-08-12 公開）としてリリース済み——`CHANGELOG.md` の
`0.1.1` 節を参照。

## ラウンド9(2026-08-13、公開後詳細レビュー)

対象リポジトリ1本+dev-standards のみの別セッションで、`0.1.1` の公開物
そのものから検証した。**検証済み(問題なし)**: tarball 同梱10ファイルに意図
しないものなし / attw 4項目すべて緑 / 公開 dist がリポジトリ HEAD の再ビルドと
バイト一致 / `build:tables`・`extract-mji.py` とも再生成で差分ゼロ /
スナップショットのハッシュ一致 / README のコード例26ブロックを公開 tarball に
対して全数実行し一致(ESM・CJS 両方)/ エラー経路(非文字列・セレクタ複数・
NFD/NFKD・孤立サロゲート・空文字列)が宣言どおり / `resolvedVia` の
ivs/svs/base/none 全経路 / キーの冪等性 / `unresolved[].index` の正当性。

**指摘と対応**(修正はいずれも修正前に再現・修正後に消えることを確認済み):

1. **HIGH 文書の統計値が別字修正前の旧テーブルの実測値のまま出荷されていた。**
   旧テーブル(40,368キー)で 40,368 / 248+581 / 251 / 1,277 / 54 が**完全に
   再現**することで出所を特定。0.1.0 は修正後のテーブルと修正前の統計を一緒に
   出荷していた。現テーブルで再実測し、README(英日)・JSDoc を
   806(=234+572)/ 343/463 / 248 / 1,246 / 47 に更新、
   `test/data-invariants.test.ts` に固定
2. **HIGH `toMatchingKey` が未知のオプションキーを黙って無視**
   (`{ normalize: "NFC" }` が例外にならず NFKC のまま)。isVariant は拒否して
   おり、options.ts のコメントは存在しない対称性を主張していた。拒否に変更
3. **MEDIUM README が isVariant/getVariants のセレクタ扱い(基底文字での参照)を
   説明していなかった。** IVS 節に追記(英日)
4. **LOW CHANGELOG/NOTES の 0.1.1 の日付が 2026-08-11 になっていた**(タグ・
   npm 公開とも実際は 2026-08-12)。修正

**このラウンドで確認できなかったもの**: README の外部リンクの到達性(作業環境の
プロキシがスコープ外ホストに 403 を返すため。npmjs.com・moji.or.jp・
GitHub の他リポジトリはこの環境から開けない)/ スループット主張の上限値
(この VM では 0.35M calls/s で、主張の 0.5–0.7M/s より遅いが、主張自体が
「桁の目安」と断っているうえ VM の性能差で説明できる範囲)。

## ラウンド10(2026-08-13、公開後詳細レビュー・第2周)

`0.1.2` を対象に、実装とは別セッションで実施。**数値はすべて現テーブルでの実測**
(README・JSDoc の該当値は `test/data-invariants.test.ts` に固定済み)。

**リリース前の全ドメイン差分**(40,295キーで公開済み 0.1.2 と突き合わせ):
`toMatchingKey` の**答えの変化0件・未解決化0件**・新規解決245件、
`reduce().unique` の**値の変化0件**・null→値 504件。純粋な追加。

**修正した内容**(`0.1.3`):

1. **HIGH 付記=別字 の除外が過剰だった。** リファレンス実装が reject するのは
   戸籍・告示582・辞書類の3カテゴリだけ(JIS包摂規準は規則1で先に確定するため
   別字処理に到達しない)なのに、全カテゴリから除外していた。コードのコメントは
   「リファレンスがそうしている」と書いており、**コメントも挙動も誤り**だった。
   候補5件(宫の宮、亮/紀/記 の IVS、𮎰 の荒)が消えていた。ラウンド6が潰した
   㐲→伏 系は3カテゴリ側なので復活していない
2. **常用漢字/人名用漢字タイブレークを追加**(所有者承認、既定の挙動)。
   IPA のリファレンス実装と同じ規則。同点806キーのうち502キーが解決し、
   `toMatchingKey` の 解決/ambiguous は 343/463 → 557/249
3. **`npm test` がリポジトリ外のチェックアウトまで拾っていた。** 作業用 worktree が
   リポジトリ内にあると vitest の既定 glob がそれを含み、111件のスイートが
   220件「成功」と報告されていた。`vitest.config.ts` で include を固定
4. 推論エッジの規模(2,999件中1,427件が告示582単独＝井/牛 と同型)と、その層だけを
   外す絞り込み方を README に明記。`isVariant` が true でも `toMatchingKey` の
   キーは揃わないこと(猫/貓)も明記

**確認できた(問題なし)**: `resolveTie`(拮抗しても全分岐が一致すれば解決する仕組み)は
**救えるのに救えていないケース0件・偽の証明0件**——差別化点は実測上守られている /
Deno 2.9.5 で公開済み実物が ESM・CJS 両方通り、full ICU も確認(未検証領域から消し込み) /
`extract-mji.py` の再生成が TSV とバイト一致 / スナップショットのハッシュ一致。

## まだ検証していない領域(ラウンド11の候補)

- **【新】非自己の JIS包摂/UCS統合候補が、順位付き候補に負ける(実測16キー)。**
  UCS統合規則が「別コードポイントと同一字」と言っている候補は、順位もホップ数も
  持たないため最下位ティアに落ちる。`宫`→共(同一字の 宮 が敗北)、`𠀍`→世(丗 が敗北)、
  `彛`→彝、`杮`→𣏕 など。README がティア設計の根拠として書いている「既に表現可能＝
  畳むのを妨げない」は**自己候補**についての説明で、この16件には当てはまらない。
  ただし `杮`/`柿` のように同一性の主張自体が微妙な例も含むため、**変えるなら
  明示的な判断が要る**(0.1.3 では現状維持し、挙動をテストで固定した)
- **【新・レビュー報告、未独立検証】民二5202号通知 正字・俗字等対照表 などの
  「種別」「付記」情報が、直列化で潰れて使われていない(該当17キー)。** ホップ数を
  持たない戸籍系候補は tier 2 に平坦化され、種別が捨てられる。IPA 実装は 5202 を
  ホップ数より上位に置く。`𥄂`⇒{直/逌} は同梱済みの根拠ビットだけでも区別できる
- 推論エッジ3,000件の**1件ずつ**の妥当性。層別の内訳(告示582単独 1,427 /
  カテゴリ横断 1,573 / 2ホップ到達不能 57)とサンプル調査までは完了
- 拮抗したまま解決しない249キー(0.1.3 でタイブレーク後)の個別妥当性。
  ラウンド10で「根拠があるのに棄権しているケース」は resolveTie 側には0件と確認済み
- Chromium 以外のブラウザ(Deno・Bun は消し込み済み)
- LICENSE / LICENSE-DATA の法的文言の逐語的な正しさ(要約であり法的助言では
  ない旨は本文に明記済み)
