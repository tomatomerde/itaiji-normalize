# NOTES

作業再開・引き継ぎ用のメモ。調査の詳細は `docs/phase0-report.md`(冒頭に
フェーズ1での訂正表あり)、変更の経緯は各コミットメッセージを参照。

## 引き継ぎ

**現在地**: `main` に v0.1.0 相当がマージ済み(PR #1)。**PR #2 がレビュー待ちで
オープン**(ブランチ `claude/verify-browser-workers`、CI 全5ジョブ green)。
npm 未公開。公開はレビューを一巡させてから判断する。

**次にやると価値が高いこと**: レビュー ラウンド6。これまで**5ラウンドすべてで
実バグが出ている**(下記の履歴参照)。特に「ある修正が別のバグを露出させる」
パターンが3回起きているため、`src/toMatchingKey.ts` / `src/reduce.ts` /
`src/ivs.ts` / `scripts/build-tables.ts` を触ったら必ず独立検証を回すこと。
未着手の領域は「まだ検証していない領域」節に列挙してある。

**動かし方**:

```sh
npm ci
npm run build:tables   # data/snapshot/ から src/generated/tables.ts を再生成
npm run build          # tsup で ESM + CJS + .d.ts
npm test               # vitest(71件)
npm run typecheck
npm run test:browser   # headless Chromium で dist を実行(要 npm run build)
npm run test:workers   # workerd(Cloudflare Workers)で dist を実行(同上)
```

ブラウザ検証で使う Chromium は `scripts/verify-browser.mjs` が解決する。環境変数
`CHROMIUM_PATH` があればそれを、無ければ既定のプリインストール先を見て、どちらも
無ければ Playwright 自身の解決に任せる(CI は `playwright install` 後のこの経路)。
Playwright が解決する版とリビジョンが合わない実行環境があるための順序で、
詳細は同ファイルの `resolveExecutablePath` のコメントにある。

**このプロジェクトの作法**(CLAUDE.md の要求。守らないと意味がない):
修正したら**修正前に再現し、修正後に消えること**を両方確認する。テストを足したら
**対象コードをわざと壊してそのテストが落ちるか**を確認する(落ちないテストは
何も守っていない)。確かめていないことは「確かめていない」と明示する。

## 現在の状態

PR #1(v0.1.0 相当)は `main` にマージ済み。PR #2(ブラウザ/Workers の実行検証 +
ラウンド5修正)がオープンで、**CI は全5ジョブ green**
(`test (20)` / `test (22)` / `browser` / `workers` / `smoke-node18`)。
`drift` は CLAUDE.md を変更する PR でのみ走る(paths フィルタ)。
npm 公開はドライランまで実施済みで、実公開は人間の作業。

レビューは4ラウンド行った(実装直後の2周 + リリース想定の監査1周 + その修正の
検証1周)。以下は各ラウンドで見つかり、修正した内容。いずれも**修正前に再現し、
修正後に消えることを確認**し、追加したテストは対象コードをわざと壊して落ちること
まで確認している。

**繰り返し起きたパターン**: ある修正が、それまで別のバグに隠されていた問題を
露出させることが2回あった(IVSキーの大文字小文字を直したら互換漢字の非収束が
現れ、SVS範囲を直したら単一文字APIの挙動が変わった)。この領域を触ったら
必ず独立検証を回すこと。

### ラウンド1・2(実装直後のレビュー)

1. **CRITICAL 非収束**: 縮退テーブルのキーに `対応するUCS`(実装を持たない
   場合もある緩い対応)を使っていたため、無関係なエントリの候補が混入し、
   歯↔齒・炉↔爐 が互いに循環していた。`実装したUCS` に変更して解消
2. **CRITICAL 多段階非収束**: 30,395件中21,116件が `reduce()` 1回では安定
   しないと判明。`toMatchingKey` に不動点反復(サイクル検出付き)を実装
3. **CRITICAL IVSテーブルが死んでいた**: TSV の大文字16進をそのまま保存する
   一方 `ivsKey()` は小文字を生成しており、全キーが不一致で常に基底へ
   フォールバックしていた。小文字化で解消
4. **CRITICAL 出力側の非冪等**: 縮退先が CJK 互換漢字(reduce 的には不動点
   だが NFKC で分解される)になる場合、キーが正規化されず 㙇 と 琢 が別キーに
   なっていた。各ホップの結果を都度正規化して解消。全40,366キーの掃引テストを追加
5. HIGH `splitUnits` が O(n²)(4万文字で11秒)→ 単一走査に書き換え(5ms)
6. MEDIUM SVS 列を読んでいなかった / `resolvedVia` 未実装 / `"cycle"` と
   `"ambiguous"` の混同 / 未解決時に変異セレクタを落とす / テストの空配列に
   よる無条件通過 / CI がテーブルのドリフトを検知しない

### ラウンド3(リリース想定の監査)

7. **CI が2ジョブとも赤**だった。`test` は Node 18 で vitest 4 が起動不能
   (rolldown が Node 20.12+ の `styleText` を要求)。`drift` は
   `DEV_STANDARDS_TOKEN` 未設定で `actions/checkout` が hard fail。
   → テストは Node 20/22 マトリクスに変更し、**`smoke-node18` ジョブを新設**
   して公開 tarball を Node 18 で `require()`/`import()` する実証に置き換えた。
   drift は secret の有無を判定してスキップするようにした
8. **CRITICAL U+FE0F を無言削除**: SVS 範囲を U+FE00–FE0F としていたため
   絵文字表示セレクタを食べて捨てていた(BMP掃引で20,335文字が脱落)。
   MJ が実際に使う SVS は U+FE00/U+FE01 だけと確認した上で U+FE00–FE0D に限定
9. **`unresolved` が機能していなかった**: MJ 外の文字をすべて報告するため
   住所1行で34文字中33文字が載り、`if (unresolved.length)` が常に発火していた。
   漢字だけを報告するよう変更(範囲の妥当性はテーブル全キーとの照合テストで固定)
10. **非文字列入力の誤答**: `reduce(["崎"])` が例外にならず、配列を `input` に
    echo した上でもっともらしい候補を返していた。4関数すべてに型ガードを追加
11. **変異セレクタ複数連結で入力に無い IVS を捏造**していた。単位としてまとめて
    消費し、2つ以上なら縮退せず素通し + `"unsupported-sequence"` 報告に変更
12. **tree-shaking が全く効かず**全利用者が 550KB(gzip)を負担していた。
    生成テーブルに `/* @__PURE__ */` を付与し、部分 import で 270–281KB に半減
    (API 全体を使う場合は 552KB のままで、README に正直に明記)
13. `toMatchingKey` が捨てる `candidates` を毎ホップ生成していた →
    内部パスを分離し 224ms → 149ms(10万件)
14. **統計値がパイプライン修正前のまま**だった(直接カバー率 74.5% → 実測
    71.4% など)。README(英日)と phase0 報告の訂正表を実測値に更新
15. **LICENSE-DATA が公開物に存在しないファイルを列挙**していた。実際に
    CC BY-SA データを含むのは `dist/index.js` / `dist/index.cjs` なので
    そちらを明記し、`PROVENANCE.md` を同梱ファイルに追加

### ラウンド4(ラウンド3の修正の検証)

16. **HIGH `toMatchingKey` の options が未検証だった**: `text` はガードしていたが
    `options` は素通しで、`"NFD"`/`"NFKD"` が `normalize()` にそのまま渡っていた。
    分解形で作ったキーは既定(NFKC)のキーと絶対に一致しないので、例外にならない
    まま永久にマッチしない = ガードを入れた目的そのものの失敗だった。
    正当な3値のホワイトリストに変更。`toMatchingKey("崎", "NFC")` のように
    options に文字列を渡すケースも黙って NFKC にフォールバックしていたので拒否する
17. **MEDIUM 部首ブロックが `unresolved` の対象外だった**: `src/cjk.ts` の範囲に
    CJK部首補助(U+2E80–2EFF)と康熙部首(U+2F00–2FDF)が無く、⺅(U+2E85)は
    NFKC マッピングも持たないため**どの正規化モードでも黙って通り抜けて**いた。
    両ブロックと拡張J(U+323B0–3347F)を追加。IDC(U+2FF0–2FFF)と
    CJK Strokes(U+31C0–31EF)は構造記法・部品であり漢字に畳まれることを
    期待する対象ではないので、理由をコードに書いた上で意図的に除外
18. **MEDIUM 抽出器が実在するシーケンス4件を落としていた**(ラウンド3以前からの
    既存バグ): 一覧表の2行が1セルに複数シーケンスを `;` 区切りで持っており
    (MJ059399 の `2B9E4_E0100;535A_E010A`)、セル全体を1キーにしていたため
    𫧤・博 の該当シーケンスがどの入力からも引けなかった。分割して 9,971 → 9,973 キーに。
    生成器に「整形式でないキーは emit を拒否する」ガードを追加し、
    全キーを公開API経由で引き直す到達性テストも追加
19. ドキュメントの精度: バンドルサイズ表は丸め境界にあるため概算であることと
    計測コマンドを明記。スループットは1台の楽観値ではなく複数マシンでの
    0.5〜0.7M/s のレンジに修正

### ラウンド5(公開前・未検証領域の総ざらい)

20. **HIGH 渡邉 が 渡辺 と一致しなかった**: 邉 の候補 辺/邊 が同点で
    `pickBest` が選択を拒否 → `"ambiguous"` として素通ししていた。だが 邊 自身も
    辺 に縮退するため**どちらの枝も 辺 に至り、選択は結果を変えない**。
    渡邊 は一致するのに 渡邉 は一致しないという、どちらか一方に倒すより悪い状態だった。
    全ての拮抗枝をたどり一致した場合のみ採用するよう変更(当て推量ではなく証明)。
    拮抗898字中345字が解決、残り553字は本当に別の不動点に至るので `"ambiguous"` のまま。
    `reduce()` は1段階APIのままで `unique: null` を返す(役割分担は不変)
21. **MEDIUM `getVariants` が根拠を借用していた**: 同一 MJ 字形の候補同士を結ぶ
    エッジに両候補のビットマスクの OR を載せていたため、`getVariants("斉")` が
    資 を `basis: moj-notice-582-appendix-4` で返していた。告示が言っているのは
    「齍 は 斉 または 資 と書ける」であって、斉 と 資 が交換可能とは言っていない。
    30,766エッジ中3,000件(9.8%)がこの種の推論。新しい `Variant` 型の
    `inferred` フラグで区別するようにした。**根拠付きであることが本パッケージの
    差別化点そのものなので、件数以上に重い**
22. **LOW(潜在) `extract-mji.py` が `<rPh>`(ふりがな)を除外していなかった**:
    `<si>` 配下の全 `<t>` を連結していたため、ルビを持つ共有文字列では
    値にルビが混入する(素朴なパースだと `実装なし` が `実装なしジッソウ` になる)。
    現スナップショットでは該当2件がどちらも抽出対象列に載らないため TSV は
    バイト一致のままだが、データ更新時に黙って値が壊れる経路だった

このラウンドで**確認できた(問題なし)**もの: `extract-mji.py` の全58,862行が
独立実装とバイト一致 / 主要な異体字姓10グループ中9グループが期待通りの挙動
(齋 と 斉 を別扱いにしているのも正しい) / `pickUnique` の順位・ホップ数の
優先順は MJ データの意図と整合(第1順位が優先形であることを確認)/
README 日英15セクションが1対1で一致 / 新しいブラウザ・Workers 検証スクリプトの
assert が意味を持つこと(Node組み込みAPIの注入・既定正規化の変更でいずれも落ちる)

## 決定済みの設計判断

- **`"cycle"` の23字は未解決のまま報告する**(2026-08-05、所有者判断)。
  址/阯、雕/鵰、羐/羑、輀/轜、㿉/㿗 などは互いに縮退し合って安定しない。
  うち14字は「自分自身」も候補に持つ(JIS包摂規準)ため循環内に安定形は存在するが、
  **両方の字が自己候補を持つ**ため、どちらを代表にするかの根拠が無い。
  名寄せキーとしては循環内の最小コードポイントを正規形にすれば 址 と 阯 を
  マッチさせられるが、それは根拠のない代表字を名乗ることになるので採らない。
  `unresolved` に `reason: "cycle"` として報告されるため、必要な利用者は
  呼び出し側で対処できる。「曖昧さを隠さない」という本プロジェクトの差別化点に一致する
- **テーブルのコンパクト再符号化は v1 では見送り**。現状 gzip 約562KB で
  目標(1MB未満)は満たしており、README の Roadmap に未実装として明記済み

## 人間の操作待ちの項目

### 完了済み(2026-08-05)

- ~~**スナップショット2点の SHA-256 照合**~~ **完了**。「配布元からの
  ダウンロード原本 / `PROVENANCE.md` の記録 / リポジトリ内の実ファイル」の
  **3ソースが完全一致**することを確認済み
- ~~**ドリフト検査用シークレットの設定**~~ **完了**。設定後に
  `workflow_dispatch` で drift ワークフローを実行し、比較ステップが実際に走って
  `Common section matches the canonical template.` を出すところまで確認した。
  **「ジョブが成功した」だけでは未設定時のスキップと区別がつかないため、
  比較が走ったことをログで確かめること**
- ~~**PR #1 のレビュー・マージ**~~ **完了**(squash merge、`48ec1b0`)
- ~~**ドリフト検査ワークフローの hard fail**~~ **完了**。テンプレート原本側で
  修正済みで、PR #6 でこのリポジトリにも取り込んだ。シークレット未設定でも
  失効していてもジョブは green のまま warning になる

### 残っているもの

- **PR #2 のレビュー・マージ**
- **npm への実公開**(`npm publish --dry-run` までは実施済み。レビューを
  一巡させてから判断する)

## まだ検証していない領域(ラウンド6の候補)

各ラウンドのレビューが「見ていない」と明言した領域:

- Deno / Bun / Chromium 以外のブラウザでの実行(Node 18/20/22・Chromium・
  workerd は CI で実行検証済み)
- 推論エッジ3,000件が個別に妥当かどうか(5件を追跡確認したのみ)
- 拮抗したまま解決しない553字の分類が個別に妥当か
- LICENSE / LICENSE-DATA の法的文言の逐語的な正しさ(要約であり法的助言では
  ない旨は本文に明記済み)
